import React, { memo } from 'react';
import './Statistics.scss';

const StatisticsInner = () => {
    return (
        <div>
            <div>123</div>
        </div>
    );
};

export const Statistics = memo(StatisticsInner);

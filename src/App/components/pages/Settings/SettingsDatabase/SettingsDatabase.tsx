import React, { memo } from 'react';
import './SettingsDatabase.scss';

const SettingsDatabaseInner = () => {
    return (
        <div className="settings-database">
            <span>123</span>
        </div>
    );
};

export const SettingsDatabase = memo(SettingsDatabaseInner);
